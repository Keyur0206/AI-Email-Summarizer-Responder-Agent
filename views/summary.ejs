<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>Email Summary</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-light">
    <div class="container py-5 text-center">
      <h2 class="mb-4 text-primary">Today's Email Summary</h2>

      <div
        class="card shadow-sm border-primary mb-3"
        style="max-width: 700px; margin: auto"
      >
        <div class="card-body">
          <ul class="list-group list-group-flush">
            <% if (summary && summary.length) { %> <%
            summary.forEach(function(line) { %> <% if(line.trim()) { %>
            <li class="list-group-item border-0 py-2"><%= line %></li>
            <% } %> <% }); %> <% } else { %>
            <li class="list-group-item border-0 py-2">
              No emails to summarize today.
            </li>
            <% } %>
          </ul>
        </div>
      </div>

      <p class="text-muted mb-4">
        Summary generated at: <%= new Date().toLocaleTimeString() %>
      </p>

      <div class="d-flex justify-content-center gap-3">
        <a href="/auth/logout" class="btn btn-danger btn-lg">Logout</a>
        <a href="/" class="btn btn-primary btn-lg">Back to Home</a>
      </div>
    </div>
  </body>
</html>
