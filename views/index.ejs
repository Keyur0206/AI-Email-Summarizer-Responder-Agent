<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <title>AI Email Assistant</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-primary">
    <div
      class="container d-flex flex-column justify-content-center align-items-center vh-100"
    >
      <h1 class="mb-5 text-center text-light">Email Assistant</h1>

      <% if (!user) { %>
      <div
        class="card p-4 shadow-sm text-center"
        style="max-width: 400px; width: 100%"
      >
        <h3 class="mb-4">Login to Get Started</h3>
        <a
          class="btn btn-primary btn-lg w-100 d-flex align-items-center justify-content-center gap-2"
          href="/auth/google"
        >
          <img
            src="https://tse4.mm.bing.net/th/id/OIP.D6P-BO32wCApcPIIjt6p5wHaHa?pid=Api&h=220&P=0"
            alt="Google Logo"
            style="width: 20px; height: 20px"
          />
          Sign in with Google
        </a>
      </div>
      <% } else { %>
      <div
        class="card p-4 shadow-sm text-center"
        style="max-width: 400px; width: 100%"
      >
        <h3 class="mb-3">Welcome, <%= user.profile.displayName %>!</h3>
        <p class="text-muted mb-4">
          Your emails are being summarized automatically.
        </p>
        <div class="d-flex justify-content-center gap-2">
          <a class="btn btn-success btn-lg" href="/email/summary"
            >View Today's Summary</a
          >
          <a class="btn btn-danger btn-lg" href="/auth/logout">Logout</a>
        </div>
      </div>
      <% } %>
    </div>
  </body>
</html>
